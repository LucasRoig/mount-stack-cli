{
  "name": "todo-change-me",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "start": "pnpm build && node --env-file=.env dist/index.mjs",
    "dev": "tsx watch --env-file=.env src/index.ts",
    "build": "tsc --noEmit && tsup",
    "check-types": "tsc --noEmit"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {},
  "devDependencies": {
    "@repo/typescript-config": "workspace:*"
  }
}